# yaml-language-server: $schema=https://schemas.recyclarr.dev/v8/config-schema.json

radarr:
  radarr-anime:
    base_url: !env_var RADARR_ANIME_BASE_URL
    api_key: !env_var RADARR_ANIME_API_KEY

    media_naming:
      folder: default
      movie:
        rename: true
        standard: standard

    quality_definition:
      type: anime

    # v8 guide-backed profile. The guide renames this profile to
    # "[Anime] Remux-1080p"; we keep the existing v7 name here so Recyclarr
    # adopts the current profile instead of creating a duplicate. Remove `name`
    # after the first successful sync if you want the guide's default name.
    quality_profiles:
      - trash_id: 722b624f9af1e492284c4bc842153a38 # [Anime] Remux-1080p
        name: Remux-1080p - Anime
        reset_unmatched_scores:
          enabled: true
